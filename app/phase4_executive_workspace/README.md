# Phase 4 — Executive Workspace

Not implemented in Phase 1.

This folder will own:

- Executive briefing assembly (multi-hop graph + RAG + longitudinal memory)
- Human-in-the-loop flags for high-stakes outputs (PDF §12)
- Scenario analysis and early-warning packages
- Cost caps for executive briefs (PDF §2)

Phase 1 already exposes `/api/chat/executive-slides` as a retrieval + LLM synthesis path. This phase replaces that with the full workspace.
